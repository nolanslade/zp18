Simulation
	Name:Sample_Config
	Output:.txt,database
	Description:Sample 3-day (plus intro) configuration file.
	Instructions:enabled  	# If not specified, enabled by default	
	Sound:enabled        	# If not specified, enabled by default
Day
	Duration:0:30         	# Minutes:Seconds
Day
	Duration:0:30
	WaterValue:1.5 		# If not specified, default is $1.00 / drop
	Impairment
		Type:Visual/Fog
		Strength:50%
Day
	Duration:1:30
	Impairment
		Type:Physical/Shake
		Strength:50%
	Treatment
		Wait			# Treatment wait time = C (c - b*current time + a*T^2)
			C:45		# Initial seconds 
			a:0.01111	# By default, 1 / day length
			b:2		# 2 by default
			c:90		# Day length by default
		Cost			# Treatment pay cost = C (c - b*current time + a*T^2)
			C:250		# Initial dollar cost
			a:default	# By default, 1 / day length
			b:default		# 2 by default
			c:default		# Day length by default